const firstName = prompt("Enter first name:");
const lastName = prompt("Enter secound name:");
const yearOfBirth = prompt("Enter year of birth:");

alert(
  "Thanks for Registration!\n" +
  "\nFirst name: " +
  firstName +
  "\nLast name: " +
  lastName +
  "\nYear of birth: " +
  (2023 - yearOfBirth)
);
